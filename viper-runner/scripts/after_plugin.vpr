field left: Int
field right: Int
  
// After plugin
method setTupleAfterPlugin(this: Ref, l: Int, r: Int)
   requires acc(this.left) && acc(this.right)
   ensures acc(this.left) && acc(this.right)
{
   this.left := l
   this.right := r
}
